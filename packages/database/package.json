{
  "name": "@media-scraper/database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": "./src/index.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "lint": "eslint src drizzle.config.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@media-scraper/shared": "workspace:*",
    "drizzle-orm": "^0.45.1",
    "postgres": "^3.4.8"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.8",
    "tsup": "^8.5.1"
  }
}
